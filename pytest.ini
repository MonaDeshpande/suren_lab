[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: requires live PostgreSQL
    e2e: full demo Reception-Analyst-Reviewer scenarios (run manually or run_e2e_demo.bat; excluded from run_tests.bat)
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestCollectionWarning
